{
  "name": "employee-portal",
  "private": true,
  "scripts": {
    "start": "concurrently -n backend,frontend -c yellow,cyan \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
